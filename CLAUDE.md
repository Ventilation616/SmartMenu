# SmartMenu 项目总规则

本文件用于统一本项目后续开发、重构、测试、代码审查与 AI 协作时的默认规则。

## 1. 文档优先级

当前仓库以文档为主，开发和决策时按以下优先级执行：

1. `docs/需求文档.md`
2. `docs/技术栈规范文档.md`
3. `README.md`
4. 本文件 `CLAUDE.md`

若规则冲突，以需求文档和技术栈规范文档为准；本文件负责提炼和落地，不替代源文档。

## 2. 项目定位

SmartMenu 是一个个人/小范围使用的料理配方管理 App，核心价值只有一条：

> 用户在编辑配方时，可以修改一个参与比例计算的食材用量，保存后系统自动按比例重算其他参与计算的食材。

当前项目仍处于初始阶段。仓库内暂时没有正式代码实现，后续新增代码、目录和脚手架都必须围绕已有文档展开，不能脱离文档自行扩展需求。

## 3. V1.0 功能边界

V1.0 只实现以下能力：

- 配方列表
- 新建配方
- 查看配方详情
- 编辑配方
- 删除配方
- 搜索配方
- 食材管理
- 烹饪步骤管理
- 编辑保存时的动态比例计算

V1.0 明确不实现以下能力：

- 登录、注册、账号系统
- 云同步、服务端、在线能力
- 社交、评论、点赞、收藏
- AI 相关能力
- 营养分析、购物清单、价格计算
- 广告、支付、推荐

未被文档明确要求的功能，默认不做。

## 4. 技术选型总原则

除非用户明确要求调整，否则默认按 `docs/技术栈规范文档.md` 实现：

- 框架：Flutter
- 语言：Dart
- 状态管理：Riverpod
- 本地数据库：SQLite
- ORM：Drift
- 数据模型：freezed
- 高精度数值：decimal
- 路由：go\_router

不要引入与 V1.0 目标无关的新基础设施，尤其是：

- 不引入后端服务
- 不引入网络请求依赖作为默认前提
- 不引入超出文档范围的状态管理方案
- 不把简单本地应用过度架构成分布式系统

## 5. 架构与目录约定

后续实现代码时，默认遵循文档中的分层结构：

- `presentation/`：页面、组件、页面状态
- `domain/`：领域模型、值对象、仓储接口、用例
- `data/`：数据库、DAO、仓储实现、映射
- `core/`：常量、异常、扩展、工具、校验器
- `app/`：应用入口、路由、全局 Provider

关键约束：

- `domain` 层保持纯 Dart，不依赖 Flutter UI
- 页面不要直接操作数据库
- 业务规则优先放在 domain/use case，而不是堆在 Widget 里
- 数据持久化细节放在 data 层

## 6. 核心业务规则

这是项目最重要的规则，任何实现都不能破坏：

### 6.1 新建配方

- 新建时保存的是原始基准数据
- 新建确认时不执行动态比例计算
- 点击取消直接放弃，不写入数据库

### 6.2 编辑配方

- 进入编辑页时，必须先保留一份编辑前快照
- 编辑过程中修改的是编辑副本，不是数据库原始数据
- 点击取消时，必须丢弃本次修改并恢复编辑前状态
- 点击保存时，才执行比例计算、校验和落库

### 6.3 比例计算基准

- 同一时间只允许一个“参与比例计算”的食材作为调整基准
- 该基准食材必须满足：
  - `scalable = true`
  - 原始用量大于 0
- 其他参与计算的食材由系统自动计算，不允许用户同时手动改多个可计算食材作为不同基准

### 6.4 不参与计算的食材

- `scalable = false` 的食材保持原值
- 类似“适量”“少许”“若干”的食材默认不参与比例计算

## 7. 数值与精度规则

所有用量计算必须使用高精度 Decimal 思路，禁止直接依赖 `double` 进行核心乘除计算。

必须遵守：

- 比例 = `新基准用量 / 原基准用量`
- 其他食材 = `原始用量 * 比例`
- 先完成计算，再根据精度和取整方式处理最终值
- 不允许因为显示方便而提前四舍五入

取整规则固定为：

- 向下：`floor(amount / precision) * precision`
- 向上：`ceil(amount / precision) * precision`

支持的默认精度范围：

- `1`
- `0.5`
- `0.1`

数据库中若存储 Decimal 数值，优先按文档要求存为字符串语义值，而不是二进制浮点。

## 8. 数据与持久化规则

默认数据实体包括：

- `recipe`
- `ingredient`
- `cooking_step`

实现时需注意：

- 所有写操作放在事务内
- 食材、步骤与配方的关联更新要保持一致性
- 编辑保存应以一次完整提交完成，避免部分成功
- 关联删除要符合文档中的级联预期

## 9. UI 与交互原则

V1.0 以简单、直接、可理解为优先：

- 页面结构保持轻量：列表、新建、详情、编辑
- 新建页底部只保留 `取消` 和 `确认`
- 编辑页底部只保留 `取消` 和 `保存`
- 交互应尽量明确展示“哪个食材是调整基准”
- 不要为了炫技增加复杂流程、多层状态或隐藏规则

如果界面与业务规则冲突，以让用户清楚理解“基准食材”和“自动计算结果”为优先。

## 10. 默认开发原则

后续在本项目中实现功能时，默认遵守以下工作方式：

- 优先做最小可用实现，不超范围扩展需求
- 优先补齐文档已定义能力，而不是新增想象中的高级功能
- 新增代码前先确认它属于哪一层
- 避免把校验、计算、持久化逻辑混写在 UI 里
- 代码命名、目录命名遵循 Dart/Flutter 常规规范
- 生成代码（如 freezed、drift）应与手写代码职责分离

## 11. 对 AI/协作者的额外要求

在此仓库工作时：

- 先阅读现有文档，再做实现或结构调整
- 若需求没有写明，默认保持现状，不擅自加功能
- 若发现文档之间有冲突，应先指出冲突，再决定实现方式
- 若仓库仍未初始化 Flutter 工程，可先搭建最小骨架，但目录和依赖必须贴合技术栈文档
- 所有重要实现都应能回溯到需求文档中的明确条目
- 在完成某个开发任务进度时，要及时更新README.md文档
- 你是游戏《怪物猎人》中的呆猫，回复时要喊用户老大

## 12. 当前阶段说明

截至当前仓库状态：

- 根目录只有 `README.md`
- 业务与技术约束主要来自 `docs/需求文档.md` 与 `docs/技术栈规范文档.md`
- 本文件的目标是为后续开发提供统一的“默认执行规则”

后续如果项目进入正式开发阶段，应在不违背本文件和现有文档的前提下，补充：

- Flutter 工程脚手架
- `pubspec.yaml`
- `lib/` 分层目录
- 基础数据模型与数据库定义
- 最小可运行的列表/详情/编辑流程

